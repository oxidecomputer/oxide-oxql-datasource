# Changelog

## 0.1.0 (Unreleased)

Initial release.

- OxQL query editor with syntax highlighting and metric name autocompletion
- Template variable support via `label_values(metric, field)` queries
- Custom legend formatting with `{{ label }}` templates
- Silo and project name enrichment from UUIDs
