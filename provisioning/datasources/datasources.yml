apiVersion: 1

datasources:
  - name: 'oxql'
    type: 'oxide-oxql-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      host: ${OXIDE_HOST}
    secureJsonData:
      apiKey: ${OXIDE_API_KEY}
